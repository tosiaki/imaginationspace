require "rails_helper"

RSpec.feature "User visits user page", :type => :feature do
  scenario "when it is the profile of another user" do
    # The user should be able to access it, and should not see the edit profile button
  end

  scenario "when it is the subscritipons list of another user" do
    # The user should have access
  end
end