require "rails_helper"

RSpec.feature "Guest visits a work page", :type => :feature do
  scenario "when there are comments to reply to" do
    # The guest should see a reply button and reply form
  end
end