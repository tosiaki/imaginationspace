<% if article.pages.count > 1 %>
	<% if page.page_number > 1 %>
		<%= link_to "🠄", show_page_user_article_path(user, article, page.page_number-1) %>
	<% else %>
		<span class="arrow-placeholder">🠄</span>
	<% end %>
	Page <span id="current-page-display"><%= page.page_number %></span>/<%= article.current_max_page %>
	<% if page.page_number < @article.pages.count %>
		<%= link_to "🠆", show_page_user_article_path(user, article, page.page_number+1) %>
	<% else %>
		<span class="arrow-placeholder">🠆</span>
	<% end %>
<% end %>