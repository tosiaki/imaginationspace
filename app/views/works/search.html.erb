<% tag_description = @search_tags.join(", ") %>
<% provide(:title, "Works matching #{tag_description}") %>
<%= render 'show_works', comics: @comics, drawings: @drawings, fancomics_heading: "#{@comics.count} Fancomics in #{tag_description}", fanart_heading: "#{@drawings.count} Fanart in #{tag_description}" %>
